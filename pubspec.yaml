name: quikservnew
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+2

environment:
  sdk: ">=3.9.0 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.2
  dio: ^5.9.0
  equatable: ^2.0.5
  dartz: ^0.10.1
  flutter_bloc: ^9.1.1
  get_it: ^7.7.0
  floor: ^1.5.0
  sqflite: ^2.4.2
  permission_handler: ^12.0.0+1
  printing: ^5.14.2
  platform: ^3.1.0
  http: ^1.5.0
  package_info_plus: ^8.3.1
  translator: ^1.0.3+1
  flutter_blue_plus: ^2.0.0
  flutter_localization: ^0.3.3
  print_bluetooth_thermal: ^1.0.0
  esc_pos_utils_plus: ^2.0.3
  intl: ^0.20.2
  fluttertoast: ^8.2.8
  path_provider: ^2.1.5
  lottie: ^3.1.2
  dartarabic: ^0.3.1
  shimmer: ^3.0.0
  device_info_plus: ^10.1.0
  fl_chart: ^1.1.1
  url_launcher: 6.1.12
  share_plus: ^7.2.1
  pdf: ^3.10.8
  flutter_svg: ^2.0.10
  image_picker: ^1.0.0
  image_cropper: ^12.2.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  build_runner: ^2.4.9
  floor_generator: ^1.5.0
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/icons/quikserv_icon.png
  min_sdk_android: 21
  

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/icons/quikserv_splash.png
    - assets/icons/quikserv_icon.png
    - assets/images/freepik__the-style-is-candid-image-photography-with-natural__16410.jpeg
    - assets/icons/quikservlogo.png
    - assets/images/WhatsApp Image 2025-12-02 at 11.45.21_c494808e.jpg
    - assets/icons/forwardicon.png
    - assets/icons/whatsapp_icon.png
    - assets/success_animation.json
    - assets/icons/Vector.png
    - assets/icons/Group (1).png
    - assets/icons/homeicon.png
    - assets/icons/settingsicon.png
    - assets/icons/Clip path group.png
    - assets/icons/Clip path group (1).png
    - assets/icons/cardicon.png
    - assets/icons/multiicon.png
    - assets/icons/salesinvoiceicon.png
    - assets/icons/paymenticon.png
    - assets/icons/salesreporticon.png
    - assets/icons/itemreporticon.png
    - assets/icons/dailyreporticon.png
    - assets/icons/reporticon(1).png
    - assets/icons/reporticon(2).png
    - assets/icons/reporticon(3).png
    - assets/icons/reporticon(4).png
    - assets/icons/reporticon(5).png
    - assets/icons/Clip path group.svg
    - assets/icons/dashboardicon.svg
    - assets/icons/homeicon.svg
    - assets/icons/menuicon.svg
    - assets/icons/cashicon.svg
    - assets/icons/cardicon.svg
    - assets/icons/multiicon.svg
    - assets/icons/salesinvoiceicon.svg
    - assets/icons/paymenticon.svg
    - assets/icons/salesreporticon.svg
    - assets/icons/itemreporticon.svg
    - assets/icons/dailyclosingicon.svg
    - assets/icons/salesreporticon1.svg
    - assets/icons/salesreporticon2.svg
    - assets/icons/salesreporticon3.svg
    - assets/icons/salesreporticon4.svg
    - assets/icons/salesreporticon5.svg
    - assets/icons/salesreportdeleteicon.svg
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
